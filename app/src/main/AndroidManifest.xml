<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stoffe.gym">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GYM">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GYM">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".dashboard.workout.WorkoutAddActivity"
            android:exported="true"
            android:theme="@style/Theme.GYM" />

        <activity
            android:name=".workout.ExerciseAddActivity"
            android:exported="true"
            android:theme="@style/Theme.GYM" />
        <activity
            android:name=".exercise.ExerciseDataAddActivity"
            android:exported="true"
            android:theme="@style/Theme.GYM" />
        <activity
            android:name=".exercise.ExerciseDataGraphActivity"
            android:exported="true"
            android:theme="@style/Theme.GYM" />
        <activity
            android:name=".dashboard.bmi.BmiAddActivity"
            android:exported="true"
            android:theme="@style/Theme.GYM" />
    </application>

</manifest>